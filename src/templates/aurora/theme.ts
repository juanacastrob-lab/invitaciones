import { TEMPLATES, templateCssVars } from '@/templates/registry';

/** Aurora vive ahora en el registro de plantillas; esto queda por compatibilidad. */
export const auroraTheme = TEMPLATES.aurora;
export const auroraCssVars = templateCssVars(TEMPLATES.aurora);
