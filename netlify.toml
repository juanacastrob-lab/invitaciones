[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# Funciones programadas (recordatorios). Conviven con el runtime de Next.
[functions]
  directory = "netlify/functions"
